# Kept as a fallback in case the site ever moves back to Netlify.
# The live host is Cloudflare Pages; this file is inert there.
# Security and cache headers live in `_headers`, which BOTH hosts read —
# one source of truth, so the two can't drift apart.

[build]
  publish = "."
  command = ""
